pywin32>=305
openai>=1.0.0
httpx>=0.25.0

# Added for v2.0 enhancements
numpy>=1.24.0
pyyaml>=6.0

# Added for v3.2 heuristic engine (Aho-Corasick keyword matching)
# Falls back to regex if not installed
pyahocorasick>=2.0.0
